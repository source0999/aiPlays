game:
  adapter: generic
  rom_path: roms/game.gb
  state_path: null
emulator:
  window: null
  speed: 0
  frame_skip: 8
  action_hold_frames: 4
  release_frames: 1
  max_episode_steps: 20000
  enabled_actions: [noop, up, down, left, right, a, b, start, select]
observation:
  mode: pixels
  grayscale: true
  width: 84
  height: 84
  frame_stack: 4
training:
  algorithm: ppo
  device: auto
  seed: 42
  num_envs: 1
  total_timesteps: 1000000
  checkpoint_frequency: 25000
  learning_rate: 0.00025
  n_steps: 128
  batch_size: 64
dashboard:
  enabled: true
  host: 127.0.0.1
  port: 8765
paths:
  models_dir: models
  logs_dir: logs
  runs_dir: runs
rewards:
  profile: exploration
  weights:
    new_coordinate: 0.1
    screen_novelty: 0.02
    step: -0.001
    stuck: -0.01
